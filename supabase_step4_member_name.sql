-- ============================================================
--  STEP4：売上・カルテに「利用者名」を保存する列を追加
--  施設の名簿は毎回変わる（入退居）ため、実績側に利用者名を残します。
--  Supabase の SQL Editor に貼り付けて Run（1回だけ・既にあってもOK）
-- ============================================================

alter table public.records add column if not exists member_name text;
