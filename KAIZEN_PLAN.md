# 改修プラン（初心者が迷わない版）― 紙1枚

作成: 2026-07-08 ／ 対象: `index.html` ／ 状態: 計画のみ（未着手）

## 目的
フリーランス初心者が **「予約 → 実績 → 売上」を説明なしで一周**できること。
運営の「全く意味がわからない」を、①売上が出ない仕組み ②開発者エラー の2点解消で潰す。

## 基本方針（言葉と画面を減らす）
- 概念を **2語だけ**に：`予定`（これから）／`実績`（終わった＝そのまま売上）。「施術終了」トグルは廃止。
- 用語を統一：施術カード/施術を記録/記録 → **「記録」**、標準メニュー/デフォルト/料金表 → **「料金表」**。
- 初日は情報を出さない：数字の壁 → **「＋予約する」1ボタン**から。
- 開発者語（Supabase / SQL）は画面から**全消し**。

---

## P0 ― これだけで大半が直る（最優先・小さい）

| # | 変更 | 対象箇所(目安) | 効果 | リスク |
|---|------|----------------|------|--------|
| 1 | **記録＝即売上**。過去日の記録は自動で実績扱い、`recDone`判定を「未来の予約だけ予定／記録は実績」に変更。手動トグル廃止 | `recDone`(341), `renderSales`(857), `renderToday`(375) | 「記録したのに0円」が消える | 集計ロジック変更→サンプルで要検証 |
| 2 | **開発者エラーをユーザー語に**。登録失敗/列不足の文言を「板谷までご連絡ください」等へ | `doSignUp`(289), `warnDropped`(327) | 詰みポイント除去 | なし（文言のみ） |
| 3 | 配布前チェック：`sql/` step1〜6 が本番DBに**全部適用済みか**確認（列不足エラーの根治） | Supabase側 | 保存エラー根絶 | DB作業（コード外） |

## P1 ― 初心者導線（中）

| # | 変更 | 対象箇所 |
|---|------|----------|
| 4 | 初回の**二重表示を解消**（ようこそ画面 と 設定シートを1つに） | `boot`(233), `firstRunGuide`(345) |
| 5 | ホームを**初日は1ボタン**に。数字カードは実績が出てから表示 | `renderToday`(382) |
| 6 | **個人フローを主導線**、施設バッチ（名簿/Excel貼付/一括）は「契約施設がある人向け」に畳む | `onboardExisting`(360), `renderClients`(815) |
| 7 | 用語をコード全体で1語に統一 | 全体 |

## P2 ― 仕上げ（後回し可）

| # | 変更 | 対象箇所 |
|---|------|----------|
| 8 | カレンダーのドラッグ移動/伸縮に明示ヒント（or 初心者は非表示） | `dayScheduleHtml`(407) |
| 9 | `TRIAL_GUIDE.md` を「URL開く→登録→予約入れる」の手順だけに簡素化 | TRIAL_GUIDE.md |

---

## 実装順序（各ステップで動作確認 → 次へ）
1. **P0-2/3**（文言＋DB確認）… 安全・即効。まずここ。
2. **P0-1**（記録＝即売上）… サンプルデータで「記録→売上に出る」を確認。
3. **P1-4/5**（初回・ホーム）… 新規アカウントで初回体験を確認。
4. **P1-6/7 → P2** … 余力で。

## 各ステップ共通のやり方（CLAUDE.md準拠）
- 着手前に **index.html のバックアップ**（テキスト保存）を板谷が取得。
- **1機能ずつ**変更 → `設定→🧪サンプルを入れる`で確認 → OKなら次へ。
- 変更後は `APP_VERSION`(161) を上げて全端末に更新バナーを出す。

## やらないこと（今回のスコープ外）
- 組織リンク（複数アカウント連携）／Supabase本移行／新テーブル追加。
