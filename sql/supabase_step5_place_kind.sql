-- ============================================================
--  STEP5：施術記録に「訪問区分（在宅／施設／病院）」の列を追加
--  「その場でサッと記録」で区分を選べるようにするため。
--  Supabase の SQL Editor に貼り付けて Run（1回だけ・既にあってもOK）
-- ============================================================

alter table public.records add column if not exists place_kind text;
